This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
controller/SyllabusController.java
dto/CreateSyllabusRequest.java
dto/SyllabusResponse.java
entity/Syllabus.java
entity/SyllabusStatus.java
repository/SyllabusRepository.java
service/SyllabusService.java
service/SyllabusServiceImpl.java
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="controller/SyllabusController.java">
package com.scse.curriculum.syllabus.controller;

import com.scse.curriculum.syllabus.dto.CreateSyllabusRequest;
import com.scse.curriculum.syllabus.dto.SyllabusResponse;
import com.scse.curriculum.syllabus.service.SyllabusService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/syllabuses")
@RequiredArgsConstructor
public class SyllabusController {

    private final SyllabusService service;

    @PostMapping
    public SyllabusResponse create(
            @Valid @RequestBody
            CreateSyllabusRequest request) {

        return service.create(request);
    }

    @GetMapping
    public List<SyllabusResponse> getAll() {
        return service.getAll();
    }

    @GetMapping("/{id}")
    public SyllabusResponse getById(
            @PathVariable Integer id) {

        return service.getById(id);
    }

    @GetMapping("/course/{courseId}")
    public List<SyllabusResponse> getByCourse(
            @PathVariable Integer courseId) {

        return service.getByCourse(courseId);
    }
}
</file>

<file path="dto/CreateSyllabusRequest.java">
package com.scse.curriculum.syllabus.dto;

import jakarta.validation.constraints.NotNull;
import lombok.Data;

@Data
public class CreateSyllabusRequest {

    @NotNull
    private Integer courseId;

    private Integer versionNumber;

    private String versionLabel;

    private String academicYear;

    @NotNull
    private Integer createdBy;

    private String changeSummary;

    private String notes;
}
</file>

<file path="dto/SyllabusResponse.java">
package com.scse.curriculum.syllabus.dto;

import lombok.Builder;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@Builder
public class SyllabusResponse {

    private Integer id;

    private Integer courseId;
    private String courseCode;
    private String courseName;

    private Integer versionNumber;
    private String versionLabel;
    private String academicYear;

    private String status;

    private Boolean isCurrent;

    private Integer createdById;
    private String createdByUsername;

    private Integer approvedById;
    private String approvedByUsername;

    private LocalDateTime submittedAt;
    private LocalDateTime approvedAt;

    private String changeSummary;
    private String notes;

    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
</file>

<file path="entity/Syllabus.java">
package com.scse.curriculum.syllabus.entity;

import com.scse.curriculum.course.entity.Course;
import com.scse.curriculum.user.entity.UserAccount;
import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDateTime;

@Entity
@Table(name = "syllabus")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Syllabus {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Integer id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "course_id")
    private Course course;

    @Column(name = "version_number")
    private Integer versionNumber;

    @Column(name = "version_label")
    private String versionLabel;

    @Column(name = "academic_year")
    private String academicYear;

    @Enumerated(EnumType.STRING)
    private SyllabusStatus status;

    @Column(name = "is_current")
    private Boolean isCurrent;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "created_by")
    private UserAccount createdBy;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "approved_by")
    private UserAccount approvedBy;

    @Column(name = "submitted_at")
    private LocalDateTime submittedAt;

    @Column(name = "approved_at")
    private LocalDateTime approvedAt;

    @Column(name = "change_summary",
            columnDefinition = "TEXT")
    private String changeSummary;

    @Column(columnDefinition = "TEXT")
    private String notes;

    @Column(name = "created_at")
    private LocalDateTime createdAt;

    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
}
</file>

<file path="entity/SyllabusStatus.java">
package com.scse.curriculum.syllabus.entity;

public enum SyllabusStatus {

    DRAFT,
    SUBMITTED,
    UNDER_REVIEW,
    REVISION_REQUESTED,
    APPROVED,
    REJECTED,
    ARCHIVED
}
</file>

<file path="repository/SyllabusRepository.java">
package com.scse.curriculum.syllabus.repository;

import com.scse.curriculum.syllabus.entity.Syllabus;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;
import java.util.Optional;

public interface SyllabusRepository
        extends JpaRepository<Syllabus, Integer> {

    List<Syllabus> findByCourseId(
            Integer courseId);

    List<Syllabus> findByStatus(
            String status);

    Optional<Syllabus> findByCourseIdAndVersionNumber(
            Integer courseId,
            Integer versionNumber);

    boolean existsByCourseIdAndVersionNumber(
            Integer courseId,
            Integer versionNumber);
}
</file>

<file path="service/SyllabusService.java">
package com.scse.curriculum.syllabus.service;

import com.scse.curriculum.syllabus.dto.CreateSyllabusRequest;
import com.scse.curriculum.syllabus.dto.SyllabusResponse;

import java.util.List;

public interface SyllabusService {

    SyllabusResponse create(
            CreateSyllabusRequest request);

    List<SyllabusResponse> getAll();

    SyllabusResponse getById(
            Integer id);

    List<SyllabusResponse> getByCourse(
            Integer courseId);
}
</file>

<file path="service/SyllabusServiceImpl.java">
package com.scse.curriculum.syllabus.service;

import com.scse.curriculum.common.exception.ResourceNotFoundException;
import com.scse.curriculum.course.entity.Course;
import com.scse.curriculum.course.repository.CourseRepository;
import com.scse.curriculum.syllabus.dto.CreateSyllabusRequest;
import com.scse.curriculum.syllabus.dto.SyllabusResponse;
import com.scse.curriculum.syllabus.entity.Syllabus;
import com.scse.curriculum.syllabus.entity.SyllabusStatus;
import com.scse.curriculum.syllabus.repository.SyllabusRepository;
import com.scse.curriculum.user.entity.UserAccount;
import com.scse.curriculum.user.repository.UserAccountRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.time.LocalDateTime;
import java.util.List;

@Service
@RequiredArgsConstructor
public class SyllabusServiceImpl
        implements SyllabusService {

    private final SyllabusRepository repository;

    private final CourseRepository courseRepository;

    private final UserAccountRepository userRepository;

    @Override
    public SyllabusResponse create(
            CreateSyllabusRequest request) {

                Integer version = request.getVersionNumber();
                if (version == null) {
                        version = 1;
                }

        if (repository.existsByCourseIdAndVersionNumber(
                request.getCourseId(),
                version)) {

            throw new RuntimeException(
                    "Syllabus version already exists");
        }

        Course course =
                courseRepository.findById(
                                request.getCourseId())
                        .orElseThrow(() ->
                                new ResourceNotFoundException(
                                        "Course not found"));

        UserAccount creator =
                userRepository.findById(
                                request.getCreatedBy())
                        .orElseThrow(() ->
                                new ResourceNotFoundException(
                                        "User not found"));

        Syllabus syllabus = Syllabus.builder()
                .course(course)
                .versionNumber(version)
                .versionLabel(
                        request.getVersionLabel())
                .academicYear(
                        request.getAcademicYear())
                .status(
                        SyllabusStatus.DRAFT)
                .isCurrent(false)
                .createdBy(creator)
                .approvedBy(null)
                .submittedAt(null)
                .approvedAt(null)
                .changeSummary(
                        request.getChangeSummary())
                .notes(
                        request.getNotes())
                .createdAt(
                        LocalDateTime.now())
                .updatedAt(
                        LocalDateTime.now())
                .build();

        return map(
                repository.save(syllabus));
    }

    @Override
    public List<SyllabusResponse> getAll() {

        return repository.findAll()
                .stream()
                .map(this::map)
                .toList();
    }

    @Override
    public SyllabusResponse getById(
            Integer id) {

        Syllabus syllabus =
                repository.findById(id)
                        .orElseThrow(() ->
                                new ResourceNotFoundException(
                                        "Syllabus not found"));

        return map(syllabus);
    }

    @Override
    public List<SyllabusResponse> getByCourse(
            Integer courseId) {

        return repository.findByCourseId(
                        courseId)
                .stream()
                .map(this::map)
                .toList();
    }

    private SyllabusResponse map(
            Syllabus syllabus) {

        return SyllabusResponse.builder()

                .id(
                        syllabus.getId())

                .courseId(
                        syllabus.getCourse().getId())

                .courseCode(
                        syllabus.getCourse().getCourseCode())

                .courseName(
                        syllabus.getCourse().getName())

                .versionNumber(
                        syllabus.getVersionNumber())

                .versionLabel(
                        syllabus.getVersionLabel())

                .academicYear(
                        syllabus.getAcademicYear())

                .status(
                        syllabus.getStatus().name())

                .isCurrent(
                        syllabus.getIsCurrent())

                .createdById(
                        syllabus.getCreatedBy().getId())

                .createdByUsername(
                        syllabus.getCreatedBy().getUsername())

                .approvedById(
                        syllabus.getApprovedBy() == null
                                ? null
                                : syllabus.getApprovedBy().getId())

                .approvedByUsername(
                        syllabus.getApprovedBy() == null
                                ? null
                                : syllabus.getApprovedBy().getUsername())

                .submittedAt(
                        syllabus.getSubmittedAt())

                .approvedAt(
                        syllabus.getApprovedAt())

                .changeSummary(
                        syllabus.getChangeSummary())

                .notes(
                        syllabus.getNotes())

                .createdAt(
                        syllabus.getCreatedAt())

                .updatedAt(
                        syllabus.getUpdatedAt())

                .build();
    }
}
</file>

</files>
